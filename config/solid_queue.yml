default: &default
  workers:
    - queues: "*"
      threads: 3
      polling_interval: 0.1
  recurring_tasks:
    daily_protein_reminder:
      class: DailyReminderJob
      schedule: "0 18 * * *"  # 6 PM every day
      queue: default
