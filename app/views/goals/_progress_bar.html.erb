<div id="goal-progress" class="w-full bg-gray-100 rounded-full h-3 mb-2">
  <div class="<%= goal.met_goal? ? 'bg-green-500' : 'bg-indigo-500' %> h-3 rounded-full transition-all"
       style="width: <%= goal.protein_progress_pct %>%"></div>
</div>
