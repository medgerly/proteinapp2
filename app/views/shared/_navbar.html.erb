<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center gap-6">
        <%= link_to "💪 Protein Tracker", authenticated_root_path, class: "text-xl font-bold text-indigo-600" %>
        <%= link_to "Dashboard", authenticated_root_path, class: "text-sm font-medium text-gray-600 hover:text-indigo-600" %>
        <%= link_to "Goals", goals_path, class: "text-sm font-medium text-gray-600 hover:text-indigo-600" %>
      </div>
      <div class="flex items-center gap-4">
        <span class="text-sm text-gray-500">
          <%= current_user.full_name %>
        </span>
        <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete },
            class: "text-sm text-gray-500 hover:text-red-600" %>
      </div>
    </div>
  </div>
</nav>
